info:
  name: login
  type: http
  seq: 2

http:
  method: POST
  url: "{{API_BASE}}/auth/login"
  body:
    type: form-urlencoded
    data:
      - name: email
        value: dakshjha8085@gmail.com
      - name: password
        value: Password123
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
